version: "3"
services:
  football-team-generator-bot:
    restart: "always"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/usr/src/app/data\
